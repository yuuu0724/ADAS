diff --git a/ADAS_seatbelt/CMakeLists.txt b/ADAS_seatbelt/CMakeLists.txt
index 6dc0ed0..1a391dd 100644
--- a/ADAS_seatbelt/CMakeLists.txt
+++ b/ADAS_seatbelt/CMakeLists.txt
@@ -1,6 +1,6 @@
 cmake_minimum_required(VERSION 3.10)
 
-project(myADAS)
+project(seatbelt)
 
 if (ENABLE_ASAN)
 	message(STATUS "BUILD WITH ADDRESS SANITIZER")
@@ -9,6 +9,12 @@ if (ENABLE_ASAN)
 	set (CMAKE_LINKER_FLAGS_DEBUG "${CMAKE_LINKER_FLAGS_DEBUG} -fno-omit-frame-pointer -fsanitize=address")
 endif ()
 
+set(CMAKE_AUTOMOC ON)
+set(CMAKE_AUTOUIC ON)
+set(CMAKE_AUTORCC ON)
+
+find_package(Qt5 COMPONENTS Widgets Gui Core REQUIRED)
+
 # 设置头文件目录
 include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
 include_directories(${CMAKE_CURRENT_SOURCE_DIR}/utils)
@@ -46,6 +52,8 @@ add_executable(${PROJECT_NAME}
     src/postprocess.cc
     src/DMSDetector.cpp
     src/threadpool.c
+    src/display.cpp
+    src/moc_display.cpp
     ${rknpu_yolov5_file}
 )
 
@@ -57,6 +65,7 @@ link_directories(${RKNNRT_LIB_PATH}) #使用 link_directories 明确指定 rknnr
 set(CMAKE_INSTALL_RPATH "$ORIGIN/../lib")
 
 find_package(Threads REQUIRED)
+find_package(Qt5 REQUIRED COMPONENTS Widgets Gui)
 target_link_libraries(${PROJECT_NAME}
     imageutils
     fileutils
@@ -67,6 +76,9 @@ target_link_libraries(${PROJECT_NAME}
     rknnrt
     Threads::Threads
     rt
+    Qt5::Widgets
+    Qt5::Gui
+    Qt5::Core
 )
 
 if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
diff --git a/ADAS_seatbelt/src/DMSDetector.cpp b/ADAS_seatbelt/src/DMSDetector.cpp
index e36c263..98db41b 100644
--- a/ADAS_seatbelt/src/DMSDetector.cpp
+++ b/ADAS_seatbelt/src/DMSDetector.cpp
@@ -12,6 +12,8 @@
 #include <cmath>
 #include <threadpool.h>
 #include <mqueue.h>
+#include "display.h"
+#include <QApplication>
 
 using namespace std;
 using namespace cv;
@@ -40,6 +42,8 @@ atomic<int> npu_index(0);
 bool use_camera = true;
 string video_path = "";
 
+DisplayWindow *displayWindow;
+
 // 使用消息队列发送音频指令
 void enqueue_audio(const string &audio_name)
 {
@@ -209,36 +213,25 @@ void *DisplayThread(void *arg)
         }
         pthread_mutex_unlock(&display_mutex);
 
-        if (frame.empty()) {
-            usleep(5000);
-            continue;
-        }
-
-        frame_count++;
-        imshow("Seatbelt ADAS", frame);
-
-        auto now = steady_clock::now();
-        double elapsed = duration_cast<duration<double>>(now - last_time).count();
-        if (elapsed >= 1.0) {
-            fps = frame_count / elapsed;
-            frame_count = 0;
-            last_time = now;
+        if (!frame.empty() && displayWindow) {
+            displayWindow->pushFrame(frame);
         }
+            usleep(5000);
 
-        char fps_text[64];
-        sprintf(fps_text, "FPS: %.1f", fps);
-        putText(frame, fps_text, Point(20, 40), FONT_HERSHEY_SIMPLEX, 1.0, Scalar(0, 255, 0), 2);
-
-        if (waitKey(display_interval_ms) == 27) {
-            system_exit = true;
-            break;
-        }
     }
     return NULL;
 }
 
-int main(int argc, char const *argv[])
+int main(int argc, char *argv[])
 {
+    QApplication app(argc, argv);
+
+    int screenWidth = 512;
+    int screenHeight = 600;
+
+    displayWindow = new DisplayWindow(screenWidth, screenHeight);
+    displayWindow->showFullScreen();
+    
     if (argc >= 2) { use_camera = false; video_path = argv[1]; }
 
     cout << "[INFO] 初始化 YOLOV5 模型..." << endl;
@@ -258,6 +251,8 @@ int main(int argc, char const *argv[])
     pthread_create(&Camera_Capture_Thread_tid, NULL, Camera_Capture_Thread, NULL);
     pthread_create(&Seat_belt_detection_thread_tid, NULL, Seat_belt_detection_thread, (void *)&pool);
     pthread_create(&DisplayThread_tid, NULL, DisplayThread, NULL);
+    
+    int ret = app.exec();
 
     pthread_join(Camera_Capture_Thread_tid, NULL);
     pthread_join(Seat_belt_detection_thread_tid, NULL);
@@ -266,5 +261,5 @@ int main(int argc, char const *argv[])
     for (int i = 0; i < NPU_CORE_NUM; i++)
         release_yolov5_model(&yolov5_ctx[i]);
 
-    return 0;
+    return ret;
 }
diff --git a/ADAS_pedestrian/CMakeLists.txt b/ADAS_pedestrian/CMakeLists.txt
index 6dc0ed0..83f0d45 100644
--- a/ADAS_pedestrian/CMakeLists.txt
+++ b/ADAS_pedestrian/CMakeLists.txt
@@ -1,6 +1,6 @@
 cmake_minimum_required(VERSION 3.10)
 
-project(myADAS)
+project(pedestrian)
 
 if (ENABLE_ASAN)
 	message(STATUS "BUILD WITH ADDRESS SANITIZER")
@@ -9,6 +9,12 @@ if (ENABLE_ASAN)
 	set (CMAKE_LINKER_FLAGS_DEBUG "${CMAKE_LINKER_FLAGS_DEBUG} -fno-omit-frame-pointer -fsanitize=address")
 endif ()
 
+set(CMAKE_AUTOMOC ON)
+set(CMAKE_AUTOUIC ON)
+set(CMAKE_AUTORCC ON)
+
+find_package(Qt5 COMPONENTS Widgets Gui Core REQUIRED)
+
 # 设置头文件目录
 include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
 include_directories(${CMAKE_CURRENT_SOURCE_DIR}/utils)
@@ -46,6 +52,8 @@ add_executable(${PROJECT_NAME}
     src/postprocess.cc
     src/DMSDetector.cpp
     src/threadpool.c
+    src/display.cpp
+    src/moc_display.cpp
     ${rknpu_yolov5_file}
 )
 
@@ -57,6 +65,7 @@ link_directories(${RKNNRT_LIB_PATH}) #使用 link_directories 明确指定 rknnr
 set(CMAKE_INSTALL_RPATH "$ORIGIN/../lib")
 
 find_package(Threads REQUIRED)
+find_package(Qt5 REQUIRED COMPONENTS Widgets Gui)
 target_link_libraries(${PROJECT_NAME}
     imageutils
     fileutils
@@ -67,6 +76,9 @@ target_link_libraries(${PROJECT_NAME}
     rknnrt
     Threads::Threads
     rt
+    Qt5::Widgets
+    Qt5::Gui
+    Qt5::Core
 )
 
 if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
diff --git a/ADAS_pedestrian/src/DMSDetector.cpp b/ADAS_pedestrian/src/DMSDetector.cpp
index cd21b28..ac301a8 100644
--- a/ADAS_pedestrian/src/DMSDetector.cpp
+++ b/ADAS_pedestrian/src/DMSDetector.cpp
@@ -12,6 +12,9 @@
 #include <cmath>
 #include <threadpool.h>
 #include <mqueue.h>
+#include "display.h"
+#include <QApplication>
+
 
 using namespace std;
 using namespace cv;
@@ -20,6 +23,8 @@ using namespace chrono;
 queue<Mat> frame_queue;
 queue<Mat> display_queue;
 
+DisplayWindow *displayWindow;
+
 static pthread_mutex_t frame_mutex = PTHREAD_MUTEX_INITIALIZER;
 pthread_cond_t frame_cond = PTHREAD_COND_INITIALIZER;
 
@@ -209,36 +214,24 @@ void *DisplayThread(void *arg)
         }
         pthread_mutex_unlock(&display_mutex);
 
-        if (frame.empty()) {
+        if (!frame.empty() && displayWindow) {
+            displayWindow->pushFrame(frame);
+        }  
             usleep(5000);
-            continue;
-        }
-
-        frame_count++;
-        imshow("Pedestrian ADAS", frame);
-
-        auto now = steady_clock::now();
-        double elapsed = duration_cast<duration<double>>(now - last_time).count();
-        if (elapsed >= 1.0) {
-            fps = frame_count / elapsed;
-            frame_count = 0;
-            last_time = now;
-        }
-
-        char fps_text[64];
-        sprintf(fps_text, "FPS: %.1f", fps);
-        putText(frame, fps_text, Point(20, 40), FONT_HERSHEY_SIMPLEX, 1.0, Scalar(0, 255, 0), 2);
-
-        if (waitKey(display_interval_ms) == 27) {
-            system_exit = true;
-            break;
-        }
     }
     return NULL;
 }
 
-int main(int argc, char const *argv[])
+int main(int argc, char *argv[])
 {
+    QApplication app(argc, argv);
+
+    int screenWidth = 512;
+    int screenHeight = 600;
+
+    displayWindow = new DisplayWindow(screenWidth, screenHeight);
+    displayWindow->showFullScreen();
+
     if (argc >= 2) { use_camera = false; video_path = argv[1]; }
 
     cout << "[INFO] 初始化 YOLOV5 模型..." << endl;
@@ -259,6 +252,8 @@ int main(int argc, char const *argv[])
     pthread_create(&Seat_belt_detection_thread_tid, NULL, Seat_belt_detection_thread, (void *)&pool);
     pthread_create(&DisplayThread_tid, NULL, DisplayThread, NULL);
 
+    int ret = app.exec();
+
     pthread_join(Camera_Capture_Thread_tid, NULL);
     pthread_join(Seat_belt_detection_thread_tid, NULL);
     pthread_join(DisplayThread_tid, NULL);
@@ -266,5 +261,5 @@ int main(int argc, char const *argv[])
     for (int i = 0; i < NPU_CORE_NUM; i++)
         release_yolov5_model(&yolov5_ctx[i]);
 
-    return 0;
+    return ret;
 }
